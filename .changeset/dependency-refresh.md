---
'@pilatos/bitbucket-cli': patch
---

Update runtime and tooling dependencies: axios 1.19, chalk 6, commander 15, and jq-wasm 3 (which cuts the bundled CLI from ~3.0 MB to ~1.8 MB). No behavioural or API changes.
